spring:
  application:
    name: auth-gateway

  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: core-service
              uri: http://localhost:8081
              predicates:
                - Path=/core/**
  profiles:
    active: docker

server:
  port: 8080